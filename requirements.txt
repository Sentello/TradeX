# Runtime dependencies — only what the app actually imports or runs.
#
# This was previously a `pip freeze` of a dev machine, which pinned ~50
# packages the app never touches (Authlib, flask-cors, psutil, coincurve and
# the whole `safety` scanner tree). Each was a package Dependabot raised
# alerts against and a human had to bump by hand, for no benefit.
#
# Transitive dependencies are intentionally left unpinned so a rebuild picks
# up their security fixes automatically.
#
# To run the tests:   pip install pytest
# To check for CVEs:  pip install pip-audit && pip-audit -r requirements.txt

ccxt==4.5.73          # exchange API client
Flask==3.1.3          # webhook receiver + dashboard
python-dotenv==1.2.2  # .env loading
bcrypt==5.0.0         # dashboard password hashing
gunicorn==26.0.0      # WSGI server for both services
